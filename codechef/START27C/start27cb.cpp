#include <bits/stdc++.h>
using namespace std;

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        float n;
        cin >> n;
        cout << ceil(n / 10);
        cout << endl;
    }
    return 0;
}
